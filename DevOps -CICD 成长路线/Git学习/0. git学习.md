好，第 8–14 天我们把 Git 从“会用命令”推进到“像工程师一样用”。
这 7 天的关键词是：**分支、协作、规范、事故现场、真实项目流**。
不是背命令，是把 Git 当成时间机器 + 多人调度系统 

---

# Git 实战训练营 · Day 8–14

> 目标：
>  能在真实团队中 **不添乱、不翻车、还能救火**

---

## Day 8｜分支模型的本质（不是命令，是思想）

### 核心认知

Git 的灵魂一句话：

> **分支不是拷贝代码，而是拷贝指针**

每个分支 = 指向某个 commit 的“书签”

### 必做实验

```bash
git init git-branch-lab
cd git-branch-lab

echo v1 > file.txt
git add .
git commit -m "v1"

git branch dev
git checkout dev
echo v2-dev >> file.txt
git commit -am "dev work"

git checkout master
```

### 观察

```bash
git log --oneline --decorate --graph --all
```

你要看懂的是：

* HEAD 在哪
* 分支指向哪个 commit
* 切分支 ≠ 拷贝文件

✅ **Day 8 达标标准**
看到 git graph，脑子里能自动画出结构

---

## Day 9｜merge vs rebase（CKA 级别必问）

### 两种合并方式的本质区别

| 操作     | 本质   | 适合   |
| ------ | ---- | ---- |
| merge  | 合并历史 | 公共分支 |
| rebase | 改写历史 | 个人分支 |

### 实战对比

```bash
git checkout dev
echo dev1 >> file.txt
git commit -am "dev1"

git checkout master
echo main1 >> file.txt
git commit -am "main1"
```

#### merge

```bash
git checkout dev
git merge master
```

#### rebase

```bash
git checkout dev
git rebase master
```

再看：

```bash
git log --oneline --graph
```

 **必须背的原则**

>  公共分支禁止 rebase
>  私人分支随便 rebase

---

## Day 10｜冲突不是事故，是日常 

### 制造冲突

```bash
git checkout -b conflict-test

# 改同一行
echo hello > a.txt
git add a.txt
git commit -m "base"

git checkout master
echo world > a.txt
git commit -am "master change"

git checkout conflict-test
echo conflict > a.txt
git commit -am "branch change"

git merge master
```

### 手工解决

```text
<<<<<<< HEAD
conflict
=======
world
>>>>>>> master
```

解决后：

```bash
git add a.txt
git commit
```

✅ **Day 10 达标**

* 不慌
* 看懂冲突标记
* 知道是谁改的、为什么冲突

---

## Day 11｜git stash（被打断的救生艇）

真实世界场景：

> 产品说：快切分支修 bug！

### 标准流程

```bash
git stash
git checkout master
# 修 bug
git checkout dev
git stash pop
```

### 进阶

```bash
git stash list
git stash apply stash@{0}
git stash drop
```

 记住：

* stash 是**栈**
* pop = apply + drop

---

## Day 12｜事故回滚三件套（救火必会）

### 场景 1：改错了但没提交

```bash
git checkout -- file.txt
```

### 场景 2：提交错了但没 push

```bash
git reset --soft HEAD~1
git reset --hard HEAD~1
```

### 场景 3：已经 push（生产事故）

```bash
git revert <commit-id>
```

⚠️ 铁律：

> **线上历史，只能 revert，不能 reset**

---

## Day 13｜多人协作标准流程（真实团队版）

### 正确姿势

```bash
git clone repo
git checkout -b feature/login

# 开发
git add .
git commit -m "feat: login api"

git pull --rebase origin master
git push origin feature/login
```

### 必懂名词

* PR / MR
* Code Review
* squash merge

 你要理解的是：

> **Git 是协作协议，不是个人工具**

---

## Day 14｜完整实战演练（毕业考）

### 模拟一个真实项目流

1️⃣ main：稳定分支
2️⃣ dev：日常开发
3️⃣ feature/login
4️⃣ hotfix/bug-001

#### 流程

```bash
git checkout -b feature/login dev
# 开发
git commit

git checkout dev
git merge feature/login

git checkout -b hotfix/bug-001 master
git commit

git checkout master
git merge hotfix/bug-001
git checkout dev
git merge master
```

最后：

```bash
git log --graph --oneline --all
```

 **Day 14 达标**

* 能复盘整个分支流
* 知道为什么要这样设计
* 不再“瞎 merge”

---

## 一句话总结 Day 8–14

> Git 从来不是记命令
> 它是 **时间线管理 + 协作秩序**

---

如果你愿意，我可以继续带你：

* 🔥 Git + GitHub PR 实战（企业级）
* 🔥 Git 面试必问 30 题
* 🔥 运维 / SRE 最常用 Git 场景
* 🔥 一次真实 Git 灾难复盘演练

你点哪条线，我就沿着那条路继续走 
