# ** Git 分支实操**
不是概念讲解，而是一步步敲命令，像在代码丛林里铺木板 你照着做一遍，分支的“空间感”会一下子立起来。

---

# Git 分支实操全流程（从 0 到 熟）

## 场景设定

你在 `main` 分支上开发，现在要做一个新功能，做完合并回主干。

---

## 一、初始化仓库（只做一次）

```bash
mkdir git-branch-demo
cd git-branch-demo
git init
```

```bash
echo "v1" > app.txt
git add app.txt
git commit -m "init app v1"
```

 当前状态：

```
main
└── init app v1
```

---

## 二、创建并切换分支（标准姿势）

```bash
git checkout -b feature/login
```

等价于：

```bash
git branch feature/login
git checkout feature/login
```

查看分支：

```bash
git branch
```

你会看到：

```text
* feature/login
  main
```

---

## 三、在分支上开发（互不干扰）

```bash
echo "login feature v1" >> app.txt
git add app.txt
git commit -m "add login feature"
```

 提交结构变成：

```
main
└── init app v1

feature/login
└── add login feature
```

---

## 四、主分支继续向前（制造分叉）

```bash
git checkout main
echo "hotfix on main" >> app.txt
git add app.txt
git commit -m "hotfix: main branch fix"
```

 此刻的世界线：

```
        feature/login
        └── add login feature
       /
init app v1
       \
        └── hotfix on main
```

---

## 五、合并分支（重点）

### 方式 1：普通 merge（会产生 merge commit）

```bash
git checkout main
git merge feature/login
```

如果没有冲突：

```text
Merge made by the 'ort' strategy
```

 结果：

```
        ┌── add login feature
        │
init ───┼── merge commit
        │
        └── hotfix on main
```

---

### 方式 2：rebase（让历史看起来像一条直线）

 **只在个人分支用**

```bash
git checkout feature/login
git rebase main
```

然后再合并：

```bash
git checkout main
git merge feature/login
```

 结果：

```
init ── hotfix ── add login feature
```

历史像被熨平了一样

---

## 六、解决冲突（必考实操）

制造冲突：

```bash
# main
git checkout main
echo "MAIN change" > conflict.txt
git add conflict.txt
git commit -m "main conflict"

# feature
git checkout feature/login
echo "FEATURE change" > conflict.txt
git add conflict.txt
git commit -m "feature conflict"
```

合并：

```bash
git checkout main
git merge feature/login
```

你会看到：

```text
CONFLICT (content): Merge conflict in conflict.txt
```

打开文件：

```text
<<<<<<< HEAD
MAIN change
=======
FEATURE change
>>>>>>> feature/login
```

手动改成你想要的：

```text
MAIN + FEATURE
```

然后：

```bash
git add conflict.txt
git commit
```

✅ 冲突解决完毕

---

## 七、删除分支（收尾）

```bash
git branch -d feature/login
```

如果 Git 不让删：

```bash
git branch -D feature/login
```

---

## 八、真实开发分支模型（你以后会天天用）

```text
main        稳定可发布
develop     日常集成
feature/*   新功能
hotfix/*    紧急修复
```

---

## 九、CKA / 面试 / 生产最爱考的 5 个点

1. `checkout -b` = 创建 + 切换
2. **合并在目标分支上做**
3. `rebase` 不碰公共分支
4. 冲突不是错，是常态
5. 合并后及时删分支

---

## 下一步你可以选一个继续练

* 🔥 **Git 分支回滚 + 恢复**
* 🧠 **rebase vs merge 对比演练**
* 💥 **误删分支如何找回**
* 🌍 **多人协作 + 远程分支实战**

你选一个，我直接带你敲到手指起茧 💻
